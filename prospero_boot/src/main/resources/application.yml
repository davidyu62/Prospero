spring:
  application:
    name: prospero-boot

server:
  port: 8080
  servlet:
    context-path: /

logging:
  level:
    root: INFO
    com.prospero: DEBUG

# API 키는 환경변수로 주입합니다 (프로젝트에 평문 키를 커밋하지 않음)
# 로컬 실행 시: export FRED_API_KEY=... 후 애플리케이션 실행
fred:
  api:
    key: ${FRED_API_KEY:}
